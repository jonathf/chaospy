[build-system]
requires = [
    "setuptools>=45",
    "setuptools_scm>=6.2",
    "setuptools_scm_git_archive",
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
version_scheme = "guess-next-dev"
